# ===================================================================
# Spring Boot configuration.
#
# This configuration is used for unit/integration tests with testcontainers database containers.
#
# To activate this configuration launch integration tests with the 'testcontainers' profile
#
# More information on database containers: https://www.testcontainers.org/modules/databases/
# ===================================================================

spring:
  ai:
    openai:
      api-key: ${OPEN_AI_API_KEY}
      chat:
        enabled: false
  datasource:
    hikari:
      auto-commit: false
      poolName: Hikari
      maximum-pool-size: 1
  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate.default_schema: flowinquiry
      hibernate.id.new_generator_mappings: true
      hibernate.connection.provider_disables_autocommit: true
      hibernate.cache.use_second_level_cache: false
      hibernate.cache.use_query_cache: false
      hibernate.generate_statistics: false
      hibernate.hbm2ddl.auto: none #TODO: temp relief for integration tests, revisit required
      hibernate.type.preferred_instant_jdbc_type: TIMESTAMP
      hibernate.jdbc.time_zone: UTC
      hibernate.timezone.default_storage: NORMALIZE
      hibernate.query.fail_on_pagination_over_collection_fetch: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: dummy
            client-secret: dummy
            scope: profile, email
            redirect-uri: http://localhost
            authorization-grant-type: authorization_code
            client-name: Google
flowinquiry:
  edition: community
  version: 1.2.4
  security:
    authentication:
      jwt:
        base64-secret: dGVzdC1zZWNyZXQtdGhhdC1pcy1iYXNlNjQtZW5jb2Rpbmctc28tY2FuLWJlLXVzZWQtZm9yLXRlc3RpbmctandzLXRva2Vucw==
        token-validity-in-seconds: 86400
        token-validity-in-seconds-for-remember-me: 2592000
